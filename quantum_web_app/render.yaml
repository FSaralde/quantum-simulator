services:
  - type: web
    name: quantum-lab
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python3 backend.py
    envVars:
      - key: OPENAI_MODEL
        value: gpt-5-mini
      - key: OPENAI_API_KEY
        sync: false
